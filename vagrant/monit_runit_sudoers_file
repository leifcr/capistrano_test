# -----------------------------------------------------------------------------------------
# Sudo entries for monit (monit_runit_rails_app)
# -----------------------------------------------------------------------------------------
deploy ALL=NOPASSWD: /usr/bin/monit *
deploy ALL=NOPASSWD: /usr/sbin/service monit *

# Sudo entries for runit (monit_runit_rails_app)
# -----------------------------------------------------------------------------------------
deploy ALL=NOPASSWD: /usr/bin/sv *

# -----------------------------------------------------------------------------------------
# Sudo entries for nginx (monit_runit_rails_app)
# -----------------------------------------------------------------------------------------
deploy ALL=NOPASSWD: /usr/sbin/service nginx *
